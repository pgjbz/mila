let array = [9 ,11];

array.push(10); //push a value

putsln("array = ", array);
let value_poped = array.pop(); //for now return remain items
putsln("value poped = ", value_poped);
putsln("array = ", array);
putsln("remove = ", array.remove(0));
putsln("array = ", array);
